export const spacing = {
  // Base spacing scale (multiples of 4)
  xs: 4,
  sm: 8,
  md: 12,
  base: 16,
  lg: 20,
  xl: 24,
  '2xl': 32,
  '3xl': 40,
  '4xl': 48,
  
  // Common padding values
  screenPadding: 16,
  cardPadding: 16,
  sectionPadding: 24,
  
  // Common margin values
  itemSpacing: 12,
  sectionSpacing: 24,
  
  // Border radius
  borderRadius: {
    sm: 4,
    base: 8,
    md: 12,
    lg: 16,
    xl: 20,
    full: 9999,
  },
  
  // Icon sizes
  iconSize: {
    sm: 16,
    base: 24,
    lg: 32,
    xl: 40,
  },
  
  // Button sizes
  buttonHeight: {
    sm: 32,
    base: 44, // iOS minimum touch target
    lg: 56,
  },
};
